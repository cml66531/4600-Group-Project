public class Chest {

	private boolean chestIsLocked;
	private Key key;
	
	public void unLock(Key theKey) {
		if(key == theKey)
			chestIsLocked = false;
	}
	
	/**
	 * Should return true if the chest is locked, false otherwise
	 */
	public boolean isLocked() {
		return chestIsLocked;
	}
	
	/**
